import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  linkContainer: {
    marginTop: '5rem',
    display: 'flex',
  },
  linkButton:{
    textDecoration:'none',
    fontSize:'1.8rem',
    marginLeft:'1rem',
  },
}))

export default useStyles;